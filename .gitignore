node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Superpowers
.superpowers/
.worktrees/

# Deep Dive API keys
keys.json

# Published jkai projects (runtime data)
data/jkai-projects/
data/whatsapp-auth/

# Webframe service
services/webframe/node_modules
services/webframe/dist

# jkai-builder bundle — produced by `npm run build:builder` from packages/jkai-builder/{bin,src}/
packages/jkai-builder/dist/

# Scraper scripts — homeserv-local runtime state (LLM-authored per-domain
# scrape scripts + metadata). Never synced to the VPS; see script-store.ts.
scraper-scripts/

# Claude Code agent scratch space
.claude/worktrees/
.claude/settings.json
.claude/skills/
