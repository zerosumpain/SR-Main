node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Superpowers
.superpowers/
.worktrees/

# Deep Dive API keys
keys.json

# Published jkai projects (runtime data)
data/jkai-projects/
data/whatsapp-auth/
data/uploads/

# Webframe service
services/webframe/node_modules
services/webframe/dist

# jkai-builder bundle — produced by `npm run build:builder` from packages/jkai-builder/{bin,src}/
packages/jkai-builder/dist/

# jkai-run-worker bundle — produced by `node packages/jkai-run-worker/build.mjs`
packages/jkai-run-worker/dist/
packages/jkai-codex-bridge/dist/

# Scraper scripts — homeserv-local runtime state (LLM-authored per-domain
# scrape scripts + metadata). Never synced to the VPS; see script-store.ts.
scraper-scripts/

# Claude Code agent scratch space
.claude/worktrees/
.claude/settings.json
.claude/skills/

# Playwright artifacts
test-results/
playwright-report/
.playwright/

# Voice corpus exports — unpublished drafts + private chat turns. See docs/voice-corpus.md
corpus.json
