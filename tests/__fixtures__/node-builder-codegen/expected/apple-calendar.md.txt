# Apple Calendar

> Read and write events on iCloud calendars via CalDAV.

## When to use

For workflows that need to read or write events on your iCloud calendar — alerts before meetings, scheduling, syncing with other systems.

## How it works

Connects via CalDAV (RFC 4791) to `caldav.icloud.com` using an app-specific password. Pick a credential, pick a calendar, choose an operation.

## Configuration

- **iCloud account** — the credential. Create one at `/admin/integrations` with kind=basic.
- **Calendar** — populated dynamically once the account is selected.
- **Operation** — list / create / update / delete.

## Output

| Operation | Output shape |
|---|---|
| list | `{ events: [{ id, ical }] }` |
| create | `{ id, url }` |
| update | `{ id, etag }` |
| delete | `{ id }` |

## Example configurations

### List events in the next 7 days

```json
{
  "credentialId": "<id>",
  "operation": "list",
  "dateRangeStart": "{{today}}",
  "dateRangeEnd": "{{nextWeek}}"
}
```

### Create a 1-hour event tomorrow at 10am

```json
{
  "credentialId": "<id>",
  "operation": "create",
  "eventTitle": "Standup",
  "eventStart": "{{tomorrow10am}}",
  "eventEnd": "{{tomorrow11am}}"
}
```

---

*Auto-generated by curate-codegen for node `apple-calendar`.*
