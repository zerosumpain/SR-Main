/**
 * Stub for SvelteKit's $env/dynamic/public and $env/static/public virtual modules.
 * Used when running standalone scripts via tsx outside the SvelteKit runtime.
 */
export const env: Record<string, string> = {};
