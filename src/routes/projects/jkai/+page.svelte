<script lang="ts">
  // Redirect handled in +page.server.ts. This file exists so SvelteKit doesn't
  // 404 the bare /projects/jkai route; published apps under [slug]/ are
  // independent static directories that don't depend on this index.
</script>
